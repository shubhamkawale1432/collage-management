<?php require_once __DIR__.'/../includes/bootstrap.php';require_role('Parent/Guardian');$q=db()->prepare('SELECT n.title,n.message,n.created_at FROM notifications n WHERE n.user_id=? ORDER BY n.id DESC');$q->execute([actor_id()]);page_top('Parent Alerts');?><div class='card p-3'><?php foreach($q as $n):?><div class='p-3 border-bottom'><strong><?=e($n['title'])?></strong><div><?=e($n['message'])?></div><small class='text-muted'><?=e($n['created_at'])?></small></div><?php endforeach;?></div><?php page_bottom();
